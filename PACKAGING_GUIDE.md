# LC GAUGE 打包和发布指南

## 📦 打包前准备

### 1. 修复图片文件名

当前 `build` 目录下的文件名不正确，需要重命名：

```
重命名前                      ➜   重命名后
────────────────────────────────────────────────
icon.ico.png                 ➜   icon.ico
installer-header.bmp.png     ➜   installer-header.bmp
installler-sidebar.bmp.png   ➜   installer-sidebar.bmp
```

**操作步骤**：
1. 进入 `d:\Projects\HPLC_improve\build` 目录
2. 删除文件名末尾的 `.png` 后缀
3. 如果原始文件是PNG格式，需要转换为正确格式：
   - `icon.ico` - 必须是ICO格式（256x256）
   - `installer-header.bmp` - 必须是BMP格式（150x57，24位色）
   - `installer-sidebar.bmp` - 必须是BMP格式（164x314，24位色）

### 2. 图片格式转换工具

**在线转换**：
- https://convertio.co/zh/
- https://onlineconvertfree.com/

**本地工具**：
- Photoshop
- GIMP (免费)
- ImageMagick (命令行)

---

## 🚀 打包应用

### 方式一：打包 Windows 安装程序（推荐）

```powershell
# 1. 构建前端
cd frontend
npm run build

# 2. 返回根目录打包
cd ..
npm run electron:build
```

生成的文件在 `dist` 目录：
- `LC GAUGE Setup 1.0.0.exe` - 完整安装程序
- `LC GAUGE 1.0.0.exe` - 便携版

### 方式二：开发模式测试

```powershell
npm run electron:dev
```

---

## 🔄 GitHub 自动更新配置

### 步骤 1：创建 GitHub 仓库

1. 登录 GitHub
2. 创建新仓库：`lc-gauge`（或你喜欢的名字）
3. 设置为 **Public**（自动更新需要公开仓库）

### 步骤 2：更新 package.json

编辑 `package.json`，将 `YOUR_GITHUB_USERNAME` 替换为你的 GitHub 用户名：

```json
"publish": {
  "provider": "github",
  "owner": "YOUR_GITHUB_USERNAME",  // ⚠️ 改成你的用户名
  "repo": "lc-gauge",
  "releaseType": "release"
}
```

### 步骤 3：生成 GitHub Token

1. 访问：https://github.com/settings/tokens
2. 点击 **Generate new token (classic)**
3. 勾选权限：`repo` (完整权限)
4. 生成并复制 Token

### 步骤 4：设置环境变量

**Windows PowerShell**：
```powershell
$env:GH_TOKEN="你的GitHub Token"
```

**或者创建 `.env` 文件**（推荐）：
```
GH_TOKEN=你的GitHub_Token
```

### 步骤 5：发布新版本

```powershell
# 打包并发布到 GitHub Releases
npm run electron:build
```

electron-builder 会自动：
1. 打包应用
2. 创建 GitHub Release
3. 上传安装包
4. 生成自动更新文件 (latest.yml)

---

## 📱 自动更新工作流程

### 用户端体验

1. **启动应用**
   - 自动检测新版本（后台进行）

2. **发现更新**
   - 弹窗提示："A new version is available. Downloading now..."
   - 自动下载更新包

3. **下载完成**
   - 弹窗提示："Update downloaded. Restart to install?"
   - 用户选择立即重启或稍后安装

4. **重启安装**
   - 应用自动关闭
   - 安装新版本
   - 自动重新启动

### 开发者发布流程

1. **修改版本号**
   ```json
   // package.json
   "version": "1.0.1"  // 更新版本号
   ```

2. **打包并发布**
   ```powershell
   npm run electron:build
   ```

3. **验证发布**
   - 访问 GitHub 仓库的 Releases 页面
   - 确认新版本已上传
   - 确认 `latest.yml` 文件存在

4. **用户自动更新**
   - 用户启动应用时自动检测新版本
   - 自动下载并提示安装

---

## 🛠️ 常见问题

### 1. 打包失败：找不到图标文件

**原因**：图片文件名或格式不正确

**解决**：
- 确保文件名完全匹配（没有 `.png` 后缀）
- 确保 `icon.ico` 是真正的 ICO 格式
- 确保 BMP 文件是 24位色深度

### 2. 自动更新不工作

**原因**：GitHub 配置不正确

**检查清单**：
- ✅ GitHub 仓库必须是 **Public**
- ✅ `GH_TOKEN` 环境变量正确设置
- ✅ `package.json` 中的 `owner` 和 `repo` 正确
- ✅ GitHub Release 中有 `latest.yml` 文件

### 3. 用户提示"未验证的发布者"

**原因**：应用未签名

**解决方案**：
- **临时方案**：用户点击"更多信息" → "仍要运行"
- **长期方案**：购买代码签名证书（约 $200-500/年）

---

## 📝 版本更新清单

每次发布新版本时：

- [ ] 更新 `package.json` 中的版本号
- [ ] 测试新功能
- [ ] 更新 CHANGELOG（如果有）
- [ ] 运行 `npm run electron:build`
- [ ] 验证 GitHub Release 已创建
- [ ] 测试自动更新功能
- [ ] 通知用户更新

---

## 🎯 下一步

1. **立即操作**：
   - 重命名 build 目录下的图片文件
   - 转换图片为正确格式
   - 测试打包：`npm run electron:build`

2. **发布前准备**：
   - 创建 GitHub 仓库
   - 配置 GitHub Token
   - 更新 package.json 中的 GitHub 用户名

3. **首次发布**：
   - 运行打包命令
   - 验证 GitHub Release
   - 测试自动更新

---

## 📞 支持

- **项目文档**：查看项目根目录的其他 `.md` 文件
- **Electron Builder 文档**：https://www.electron.build/
- **electron-updater 文档**：https://www.electron.build/auto-update

---

**LC GAUGE** - Greenness Assessment Unified Generalised Evaluator  
© 2025 Dalian University of Technology
